---
description: Linear-first project management workflow for Undefault
alwaysApply: true
---

# Linear Project Management

- Treat Linear as the source of truth for product scope. This workspace's Linear MCP is Undefault (team `UND`, project `undefault`). If MCP ever points at another product, do not file Undefault work there; use [docs/roadmap.md](docs/roadmap.md) `PIVOT-*` IDs instead.
- Current product MVP is CS2 + Tauon control (UND-82). Spotify product work is canceled; leftover Spotify is excision only (UND-84). Do not file Spotify adapters, OAuth, or apps.
- When the user asks to manage work, prefer Linear (or the in-repo roadmap if Linear is unavailable) over chat-only plans.
- Do not create implementation tasks from design tasks until the product owner approves the design/proposal.
- Keep implementation work delegated from specific Linear issues; update issue status before and after delegated work.
- Use `Todo` for not-started work, `In Progress` for active management or implementation, and `Done` only after acceptance criteria are verified.
- Add clear acceptance criteria to every task before assigning or delegating it.
- Do not commit, push, or create PRs unless the user explicitly asks.
