---
description: Hotkeys + Timeline implementation and documentation boundaries
globs: GsiHost/**/*.cs, GsiHost.Tests/**/*.cs, Core/**/*.cs, Core.Tests/**/*.cs, docs/**/*.md
alwaysApply: false
---

# Hotkeys + Timeline

- Treat manual actions as intent capture for testers/product owner, not end-user client functionality.
- Manual actions must not create `NormalizedEvent` values and must not route through `RulesEngine.ActionMap`.
- Timeline entries should preserve source: `gsi` for gameplay events and `user_action` for manual intent.
- `POST /gsi/reset` is a session boundary: detector/snapshot state, recent events, timeline memory, and JSONL session should reset together.
- Keep `custom:*` manual event keys explicit in `control-profiles.json`.
- Disabled manual actions should record a `disabled` outcome and avoid player calls.
- Add regression coverage for no double-fire between manual action paths and GSI-driven rules.

## Product pivot (2026-08-14)

Automation via rules → `IMusicPlayer` is the product MVP. `--intent-capture` observe+record is leftover tester tooling, not the Tauon slice. `--mvp` is rejected at startup. See `product-boundaries.mdc`.

## Historical (2026-06-28)

UND-64 treated observe+record as an end-user MVP exception. That product direction is closed. Remaining timeline code must not grow a second Spotify/Tauon writer next to `RulesEngine`.
